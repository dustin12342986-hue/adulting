ADULTING  v2026.08.16c — gateway fallback fix

TWO THINGS TO DO
  1. Upload these files (from INSIDE this folder, not the folder itself)
       repo root:  index.html, manifest.json, sw.js, icon-192.png, icon-512.png
       js folder:  app.js, state.js, budget-engine.js, reentry.js,
                   blue-bonnet-widget-organize.js

  2. Put your Worker Proxy URL back in the app:
       Settings -> Blue Bonnet Assistant -> Worker Proxy URL
       https://bluebonnetproxy.dustin12342986.workers.dev
     It was lost when site data got cleared. This is what gives Blue Bonnet
     its powers back (budgets, bills, statements).

  Optional: line 43 of js/blue-bonnet-widget-organize.js still has
  PUT_YOUR_GATEWAY_KEY_HERE. Fill it in to enable the free-provider backup.

WHAT WAS WRONG
  "Couldn't reach the assistant" appeared because no Worker Proxy URL was
  saved. But it should never have dead-ended like that — the gateway was
  right there. The fallback only triggered when Anthropic FAILED, not when it
  was missing entirely.

  Now: no Anthropic configured + gateway key present = it just uses the
  gateway, tells you it's in brain building mode, and mentions that adding the
  Worker URL in Settings restores full function. It only shows a hard error
  when neither is available.
