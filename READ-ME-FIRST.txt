ADULTING  v2026.08.16f — honest error messages

WHY THIS KEPT SAYING THE SAME THING
  That "Set your Worker Proxy URL" message was a catch-all. It was shown for
  EVERY failure, whatever the actual cause. Since v16e baked the URL in, the
  URL was never the problem — the message was just hiding whatever really
  went wrong.

  It now names the actual cause:
    401 / 403  -> Anthropic rejected the API key (check ANTHROPIC_API_KEY in
                  your Cloudflare Worker's settings)
    402 / credit -> out of Anthropic credit
    429        -> rate limited, wait a minute
    5xx        -> Worker error, check Cloudflare logs
    network    -> couldn't reach the Worker at all
  and every message now includes the raw error underneath.

  My money is on the API key or credit. The next message will tell you which.

ALSO FIXED
  "Brain building mode" was never actually appearing. One of my edits silently
  failed to match, so the gateway fallback ran but the banner explaining it
  never rendered — and the interaction ID never reached the thumbs, so ratings
  on those replies went nowhere. Both now work.

FILES
  repo root:  index.html, manifest.json, sw.js, icon-192.png, icon-512.png
  js folder:  app.js, state.js, budget-engine.js, reentry.js,
              blue-bonnet-widget-organize.js
  Upload from INSIDE this folder. Sidebar should read v2026.08.16f.
