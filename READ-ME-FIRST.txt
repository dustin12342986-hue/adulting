ADULTING  v2026.08.16g — gateway live, no Anthropic credit needed

Your gateway key is installed. Just upload and it works.

  repo root:  index.html, manifest.json, sw.js, icon-192.png, icon-512.png
  js folder:  app.js, state.js, budget-engine.js, reentry.js,
              blue-bonnet-widget-organize.js

Upload from INSIDE this folder. Hard-refresh. Sidebar reads v2026.08.16g.

WHAT HAPPENS NOW
  Blue Bonnet tries Anthropic, gets the out-of-credit error, and immediately
  re-asks your gateway (Groq / Gemini). You get an answer either way. Replies
  that came from the gateway are labelled "Brain building mode".

  Tested against a simulated out-of-credit response: falls back, answers, and
  never shows you the credit error.

WHAT STILL NEEDS ANTHROPIC CREDIT
  Adding bills, building budgets, logging transactions, reading statements.
  Those use tool-calling, which the free providers don't support. Blue Bonnet
  will say so rather than pretending it did something.

  Add credit whenever you want those back — the gateway then just sits there
  as a safety net for the next outage.
