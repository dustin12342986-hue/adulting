ADULTING  v2026.08.16d — settings autosave

DO THIS FIRST (no upload needed, fixes it right now)
  Settings -> scroll to the "Blue Bonnet Assistant" CARD
  Paste into Worker Proxy URL:
      https://bluebonnetproxy.dustin12342986.workers.dev
  Click the Save button INSIDE THAT CARD — not the one higher up the page.
  You should see a "Saved" toast.

WHY IT KEPT NOT STICKING
  The Settings page has three separate forms, each with its own Save button.
  Pasting into the Blue Bonnet field and then clicking the General or Calendar
  Save button silently discards it — the paste looks like it worked and isn't
  stored anywhere.

FIXED IN THIS BUILD
  The Worker Proxy URL now saves the moment you click away from the field.
  No Save button needed, no way to lose it by pressing the wrong one. It also
  pulls the URL out if you paste a whole sentence around it.

FILES
  repo root:  index.html, manifest.json, sw.js, icon-192.png, icon-512.png
  js folder:  app.js, state.js, budget-engine.js, reentry.js,
              blue-bonnet-widget-organize.js
  Upload from INSIDE this folder. Sidebar should then read v2026.08.16d.
